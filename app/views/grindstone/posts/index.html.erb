<h1>Listing Posts</h1>

    <% @posts.each do |post| %>
      <h2><%= post.title %></h2>
      <h5><% puts post.author %></h5>
      <i><%= post.created_at %></i>
      <div><%= post.body %></div>
      <%= link_to 'Show', post %>
    <% end %>